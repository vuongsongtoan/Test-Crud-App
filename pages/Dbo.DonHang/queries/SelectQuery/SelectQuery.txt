SELECT * FROM dbo.DonHang
WHERE ID_don_hang like '%{{data_table.searchText || ""}}%'
ORDER BY {{data_table.sortOrder.column || 'ID_don_hang'}} {{data_table.sortOrder.order || "ASC"}}
OFFSET {{(data_table.pageNo - 1) * data_table.pageSize}} ROWS
FETCH NEXT {{data_table.pageSize}} ROWS ONLY;
